# Firmware Development Archive

This directory preserves incremental test sketches and earlier development milestones for engineering traceability.

The completed and verified firmware is maintained at:

```text
arduino/gate_controller/gate_controller.ino
```

Do not use the archived sketches as the final wiring or deployment authority. Use the current guides in `arduino/docs/`.
